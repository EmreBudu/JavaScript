function fixAge(arr) {
  let newArr = [];
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] > 18 && arr[i] < 60) {
      newArr.push(arr[i]);
    }
  }
  return newArr;
}

fixAge([2, 12, 8, 14, 80, 0, 1,50,59,58,19,77,51,47]);
